body {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center; /* Centers the content */
  min-height: 100vh;
  overflow: hidden; /* Prevents unnecessary scrolling */
  background-color: #000000; /* Black background */
  font-family: Arial, sans-serif;
  color: #ffffff;
}

h1 {
  font-size: 42px;
  text-align: center;
}

/* Table styling */
table {
  border-collapse: collapse;
  width: 80%;
  margin: auto;
  background-color: #1a1a1a; /* Dark table background */
  color: #ffffff; /* Light text */
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
}

.table-container {
  width: 80%;
  max-height: 80vh; /* Limits height but allows scrolling */
  overflow-y: scroll; /* Enables scrolling */
  scrollbar-width: none; /*Hides scrollbar in Firefox */
  display: flex; /* Enables flexbox */
  justify-content: center; /* Centers horizontally */
  align-items: center; /* Centers vertically */
  margin: auto; /* Ensures centering */
  flex-direction: column; /* Stacks content properly */
}

.table-container::-webkit-scrollbar {
  /*display: none;  Hides scrollbar in WebKit browsers */
  width: 10px; /* Scrollbar width */
}

/* Table header */
th {
  background-color: #bfb59d; /* Beige header background */
  color: #000000; /* Black text for contrast */
  padding: 15px;
  text-align: center;
}

/* Table rows and cells */
td {
  padding: 10px;
  border-bottom: 1px solid #333333; /* Subtle border */
  text-align: center;
}

tr:nth-child(even) {
  background-color: #1f1f1f; /* Slightly lighter rows for contrast */
}

/* Add transaction button */
.add-transaction {
  display: block;
  margin: 20px auto;
  padding: 10px 20px;
  background-color: #bfb59d; /* Beige button */
  color: #000000; /* Black text */
  border: none;
  border-radius: 5px;
  font-size: 16px;
  cursor: pointer;
  transition: background-color 0.3s ease;
  max-width: 200px;
}

.add-transaction:hover {
  background-color: #7e7975; /* Darker beige on hover */
}

.edit-button {
  background: transparent;
  border: none;
  cursor: pointer;
  font-size: 20px; /* Adjust icon size */
  color: #bfb59d; /* Match the theme */
}
.edit-button:hover {
  color: #7e7975; /* Change color on hover */
}

.table-container::-webkit-scrollbar-track {
  background: #222; /* Dark track */
  border-radius: 5px;
}

.table-container::-webkit-scrollbar-thumb {
  background: #444; /* Dark gray thumb */
  border-radius: 5px;
}

.table-container::-webkit-scrollbar-thumb:hover {
  background: #666; /* Lighter gray when hovered */
}
